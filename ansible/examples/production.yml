# Plantilla de inventory/production.yml (ese fichero es privado y no se sube).
all:
  children:
    web:
      hosts:
        servidor:
          ansible_host: 203.0.113.10      # IP o nombre del servidor
          ansible_user: usuario           # usuario SSH con sudo
